# Brief histórico del kit

Estado: histórico; no usar como definición vigente del producto.

Este archivo quedó como plantilla del kit y no contiene el brief real de Venturino. Para comprender el sistema actual, usar en este orden:

1. `docs/ai/PROJECT_CONTEXT.md` para propósito, stack, módulos y flujo de información.
2. `docs/technical/arquitectura.md` para arquitectura y operación.
3. `docs/technical/referencias-mercado-matching.md` para el algoritmo de comparativos de Padawanway.
4. `docs/technical/postventa-ml.md` y `docs/technical/superadmin-observabilidad.md` para sus módulos específicos.

No completar esta plantilla retrospectivamente. Si se inicia un hito nuevo que requiere un brief, crear un documento fechado en `docs/changes/` o `docs/product/` y registrarlo en `docs/ai/MANIFEST.md`.
